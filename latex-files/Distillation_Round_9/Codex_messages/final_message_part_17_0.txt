Updated the target theorem in [formalizations.tex](/local/atle/Codex-Run/formalizations.tex:13107) only after line `13105`. The verified shortenings are at lines `13135-13148`, `13211-13215`, and `13244-13246`: I removed one nested wrapper around the open-covering argument, compressed the `X = \emptyset` case into one guided step, and merged the `B` image facts into a single line.

I also did 20+ local shortening rounds in that region, including several attempted compressions that I reverted because they caused parse errors, countermodels, or prover timeouts in the remaining contradiction-heavy / `byCase` blocks. Final verification passed with `python3 -m quick_verifications -f formalizations.tex` because this workspace does not have a `python` executable on `PATH`.
